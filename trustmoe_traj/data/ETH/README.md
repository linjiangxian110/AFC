# ETH 原始数据目录说明

本目录用于存放 TrustMoE-Traj 第一版使用的 ETH 数据集原始文件。

当前按五个子集划分目录：

- `eth/`
- `hotel/`
- `univ/`
- `zara1/`
- `zara2/`

后续建议：

1. 将每个子集的原始文件直接放入对应子目录；
2. 保持原始文件不被覆盖，预处理结果单独存放；
3. 后续补充一个数据检查脚本，对缺失文件、命名格式、split 配置进行校验。