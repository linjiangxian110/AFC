# Main TrustMoE-Traj / AFC environment.
#
# Python 3.10 is recommended. Install a CUDA-matched PyTorch build from
# https://pytorch.org/get-started/locally/ when GPU execution is required.

torch>=2.1
numpy>=1.24
pandas>=2.0
matplotlib>=3.7
PyYAML>=6.0
dill>=0.3.7
GitPython>=3.1
scipy>=1.10
einops>=0.7
